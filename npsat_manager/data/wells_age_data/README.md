# Integration of NPSATAge Dataset

Dumped npsatDump1.sql into a MySQL database, then exported each table to individual CSV files.

The exported data includes all the same wells, using the same eid values as Mantis, with additional attributes. The dataset also includes urf points associated with each well.
